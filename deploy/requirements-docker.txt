# Docker/runtime requirements (single service: FastAPI + Streamlit)
#
# IMPORTANT: avoid downloading huge CUDA/NVIDIA wheels by forcing CPU-only torch.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.2.2

# Core app deps
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.0.0
requests>=2.28.0
httpx>=0.26.0
python-dotenv>=1.0.0

# Document parsing
PyPDF2>=3.0.0
python-docx>=0.8.11

# RAG + embeddings
openai>=1.0.0
langchain>=0.0.300
pinecone-client>=2.2.1
sentence-transformers>=2.7.0
transformers>=4.40.0
huggingface-hub>=0.24.0,<1.0

# UI + export
streamlit>=1.24.0
fpdf2>=2.6.0
pandas>=1.5.0
tqdm>=4.64.0
